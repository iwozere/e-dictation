import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder — replace with real tests', (tester) async {
    // TODO: add widget tests once Supabase is initialised in test helpers.
    expect(true, isTrue);
  });
}
